<!DOCTYPE html>
<html>
  <head>
    <title>Hackmate Welcome Email</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Exo+2:wght@300;400;500;600&display=swap"
      rel="stylesheet"
    />
    <style type="text/css">
      /* Base Cyberpunk Styles */
      body {
        margin: 0;
        padding: 0;
        min-width: 100%;
        font-family: "Exo 2", Arial, sans-serif;
        font-size: 16px;
        line-height: 1.5;
        background-color: #0f0f13; /* Deep dark background */
        color: #e0e7ff; /* Bright text */
        background-image: radial-gradient(
            circle at 10% 20%,
            rgba(139, 92, 246, 0.1) 0%,
            transparent 20%
          ),
          radial-gradient(
            circle at 90% 80%,
            rgba(99, 102, 241, 0.1) 0%,
            transparent 20%
          );
      }

      a {
        color: #00ffff; /* Neon cyan */
        text-decoration: none;
        transition: all 0.3s ease;
      }

      a:hover {
        color: #ff00ff; /* Neon magenta */
        text-shadow: 0 0 8px rgba(255, 0, 255, 0.7);
      }

      h1 {
        font-family: "Orbitron", monospace;
        font-size: 26px;
        font-weight: 700;
        line-height: 1.25;
        margin-top: 0;
        margin-bottom: 15px;
        text-align: center;
        color: #ffffff;
        text-shadow: 0 0 10px rgba(0, 255, 255, 0.7);
      }

      p {
        margin-top: 0;
        margin-bottom: 20px;
      }

      table td {
        vertical-align: top;
      }

      /* Wrapper */
      .email-wrapper {
        max-width: 600px;
        margin: 0 auto;
        background: rgba(15, 15, 19, 0.8); /* Glass background */
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border: 1px solid rgba(99, 102, 241, 0.2); /* Glass border */
        box-shadow: 0 10px 40px -10px rgba(0, 0, 0, 0.8);
      }

      /* Header */
      .email-header {
        background: linear-gradient(
          135deg,
          rgba(0, 255, 255, 0.1),
          rgba(139, 92, 246, 0.2)
        );
        padding: 24px;
        color: #ffffff;
        position: relative;
        overflow: hidden;
      }

      .email-header::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 2px;
        background: linear-gradient(
          90deg,
          transparent,
          #00ffff,
          #ff00ff,
          #00ffff,
          transparent
        );
        animation: scanline 3s linear infinite;
      }

      /* Body */
      .email-body {
        padding: 24px;
        background: rgba(15, 15, 19, 0.6); /* Glass card */
        border-radius: 0 0 12px 12px;
      }

      /* Footer */
      .email-footer {
        background: rgba(15, 15, 19, 0.8); /* Dark glass */
        padding: 20px;
        border-top: 1px solid rgba(99, 102, 241, 0.2);
      }

      /* Button */
      .button {
        display: inline-block;
        background: linear-gradient(
          135deg,
          #00ffff,
          #8b5cf6
        ); /* Cyan to purple */
        color: #0f0f13;
        font-family: "Orbitron", monospace;
        font-size: 16px;
        font-weight: 700;
        text-align: center;
        text-decoration: none;
        padding: 12px 24px;
        border-radius: 6px;
        box-shadow: 0 0 20px rgba(0, 255, 255, 0.5);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
      }

      .button::before {
        content: "";
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(
          90deg,
          transparent,
          rgba(255, 255, 255, 0.4),
          transparent
        );
        transition: left 0.5s;
      }

      .button:hover {
        transform: translateY(-2px);
        box-shadow: 0 0 30px rgba(0, 255, 255, 0.8);
      }

      .button:hover::before {
        left: 100%;
      }

      .footer-text {
        color: #a1a1aa;
        font-size: 14px;
        text-align: center;
        margin: 0;
      }

      /* Cyberpunk Elements */
      .cyber-line {
        height: 1px;
        background: linear-gradient(
          90deg,
          transparent,
          rgba(0, 255, 255, 0.5),
          rgba(255, 0, 255, 0.5),
          transparent
        );
        margin: 20px 0;
      }

      .hackmate-logo {
        text-align: center;
        margin-bottom: 20px;
      }

      .hackmate-logo span {
        font-family: "Orbitron", monospace;
        font-weight: 900;
        font-size: 28px;
        background: linear-gradient(135deg, #00ffff, #ff00ff);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        text-shadow: 0 0 10px rgba(0, 255, 255, 0.5);
      }

      /* Animations */
      @keyframes scanline {
        0% {
          transform: translateX(-100%);
        }
        100% {
          transform: translateX(100%);
        }
      }

      /* Responsive */
      @media (max-width: 480px) {
        .email-wrapper {
          margin: 10px;
        }

        .email-body,
        .email-header,
        .email-footer {
          padding: 15px;
        }

        h1 {
          font-size: 22px;
        }
      }
    </style>
  </head>
  <body>
    <div class="email-wrapper">
      <!-- Header -->
      <div class="email-header">
        <div class="hackmate-logo">
          <span>HACKMATE</span>
        </div>
        <h1>👋 Welcome to Hackmate, <%= user.name %>!</h1>
      </div>

      <!-- Body -->
      <div class="email-body">
        <p>Hello <%= user.name %>,</p>
        <p>
          We're excited to have you on board at <b>Hackmate</b>! 🚀 Get ready to
          connect, collaborate, and build something amazing with your team.
        </p>
        <p>
          You can now access your Hackmate dashboard to explore hackathons, find
          teammates, and start innovating right away.
        </p>

        <div class="cyber-line"></div>

        <p style="text-align: center; margin: 30px 0">
          <a href="<%= websiteLink %>" class="button">ACCESS DASHBOARD</a>
        </p>

        <div class="cyber-line"></div>

        <p>If you didn't sign up for Hackmate, please ignore this email.</p>
      </div>

      <!-- Footer -->
      <div class="email-footer">
        <p class="footer-text">
          Need help? Reach out to us at
          <a href="mailto:support@hackmate.com">support@hackmate.com</a>
        </p>
        <p class="footer-text">
          © <%= new Date().getFullYear() %> Hackmate. All rights reserved.
        </p>
      </div>
    </div>
  </body>
</html>

